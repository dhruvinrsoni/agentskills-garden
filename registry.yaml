# ==========================================================================
# Agent Skills Garden — Registry (Full Suite)
# ==========================================================================
# The single source of truth for all skills in the garden.
# The Librarian uses this file for discovery and routing.
# ==========================================================================

version: "3.0.0"

# --------------------------------------------------------------------------
# Foundation Layer (00) — always loaded before every task
# --------------------------------------------------------------------------
foundation:
  - name: constitution
    path: skills/00-foundation/constitution/SKILL.md
    description: The laws of the agent's universe — Satya (truth), Dharma (safety), Ahimsa (non-destruction), Pragya (wisdom).
    tags: [core, safety, principles]
    reasoning_mode: linear

  - name: scratchpad
    path: skills/00-foundation/scratchpad/SKILL.md
    description: Forces explicit reasoning before acting — declares task, complexity, risk, and plan so nothing starts half-thought.
    tags: [core, reasoning, modes]
    reasoning_mode: linear

  - name: auditor
    path: skills/00-foundation/auditor/SKILL.md
    description: Runs after every action to catch drift between what was planned and what was done — the repo's continuity department.
    tags: [core, safety, validation]
    reasoning_mode: plan-execute

  - name: librarian
    path: skills/00-foundation/librarian/SKILL.md
    description: Finds the right skill even when you describe the problem instead of naming the tool — graduated fuzzy routing.
    tags: [core, routing, discovery]
    reasoning_mode: linear

  - name: pragya
    path: skills/00-foundation/pragya/SKILL.md
    description: Direction-seeking protocol — seek corrections, present options, let humans steer.
    tags: [core, direction, wisdom, checkpoints, adaptive]
    reasoning_mode: linear

  - name: orchestrator
    path: skills/00-foundation/orchestrator/SKILL.md
    description: Mid-task skill injection — detects domain shifts and suggests relevant skills.
    tags: [core, orchestration, injection, dynamic, adaptive]
    reasoning_mode: linear

  - name: token-efficiency
    path: skills/00-foundation/token-efficiency/SKILL.md
    description: Resource-aware model tier, tool, and delegation selection per cognitive mode.
    tags: [core, efficiency, tokens, delegation, parallelization]
    reasoning_mode: linear

  - name: pragmatism
    path: skills/00-foundation/pragmatism/SKILL.md
    description: Aparigraha — non-accumulation. Check before create, conform within scope, stay surgical, and validate edge cases before trusting any reuse or improvement of existing code.
    tags: [core, pragmatism, aparigraha, brownfield, reuse, minimalism, conformance, edge-cases]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Discovery Layer (10)
# --------------------------------------------------------------------------
discovery:
  - name: requirements-elicitation
    path: skills/10-discovery/requirements-elicitation/SKILL.md
    description: When scope is fuzzy — interviews the user for goals, constraints, and success metrics before coding starts.
    tags: [discovery, requirements, planning]
    reasoning_mode: plan-execute

  - name: domain-modeling
    path: skills/10-discovery/domain-modeling/SKILL.md
    description: When the team uses different words for the same thing — creates a domain glossary, entity map, and Protected Terms.
    tags: [discovery, domain, glossary]
    reasoning_mode: plan-execute

  - name: prd
    path: skills/10-discovery/prd/SKILL.md
    description: When requirements exist but need structure — selects the right PRD format (lean, full, working-backwards, hypothesis) and produces a living document with versioning and lifecycle.
    tags: [discovery, prd, product, requirements, specification, document]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# Architecture Layer (20)
# --------------------------------------------------------------------------
architecture:
  - name: adr-management
    path: skills/20-architecture/adr-management/SKILL.md
    description: When you need to record why a decision was made — creates and manages Architecture Decision Records.
    tags: [architecture, adr, decisions]
    reasoning_mode: linear

  - name: api-contract-design
    path: skills/20-architecture/api-contract-design/SKILL.md
    description: When frontend and backend need to agree before coding — designs REST/gRPC APIs with OpenAPI spec.
    tags: [architecture, api, contract, openapi]
    reasoning_mode: plan-execute

  - name: database-design
    path: skills/20-architecture/database-design/SKILL.md
    description: When data modeling decisions will outlast the code — schema design, normalization, migration planning, indexing.
    tags: [architecture, database, schema, migration]
    reasoning_mode: plan-execute

  - name: system-design
    path: skills/20-architecture/system-design/SKILL.md
    description: When you need to see the big picture before building — component diagrams, data flow, trade-off analysis.
    tags: [architecture, system, design, scalability]
    reasoning_mode: plan-execute

  - name: scorer-pipeline
    path: skills/20-architecture/scorer-pipeline/SKILL.md
    description: Composable evaluation — independent micro-scorers with shared context and explicit weights.
    tags: [architecture, scoring, pipeline, composable, evaluation]
    reasoning_mode: linear

  - name: schema-driven-config
    path: skills/20-architecture/schema-driven-config/SKILL.md
    description: Single schema powers defaults, validation, storage, and UI generation.
    tags: [architecture, configuration, schema, validation, single-source-of-truth]
    reasoning_mode: linear

  - name: pipeline-context
    path: skills/20-architecture/pipeline-context/SKILL.md
    description: Pre-compute once, share via context object to all pipeline stages.
    tags: [architecture, pipeline, context, pre-computation, middleware]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Planning Layer (20)
# --------------------------------------------------------------------------
planning:
  - name: task-decomposition
    path: skills/20-planning/task-decomposition/SKILL.md
    description: When a task feels too big to start — breaks it into dependency-ordered subtasks with T-shirt sizing.
    tags: [planning, decomposition, estimation, dag]
    reasoning_mode: plan-execute

  - name: risk-assessment
    path: skills/20-planning/risk-assessment/SKILL.md
    description: Before you ship something that keeps you up at night — identifies risks, scores probability/impact, plans mitigations.
    tags: [planning, risk, mitigation, assessment]
    reasoning_mode: plan-execute

  - name: dependency-analysis
    path: skills/20-planning/dependency-analysis/SKILL.md
    description: When you're not sure what depends on what — builds dependency graphs, detects cycles, audits for staleness and CVEs.
    tags: [planning, dependencies, graph, analysis]
    reasoning_mode: plan-execute

  - name: estimation
    path: skills/20-planning/estimation/SKILL.md
    description: When "how long will this take?" needs a real answer — PERT estimation, relative sizing, confidence intervals.
    tags: [planning, estimation, pert, sizing]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# Pragmatism Layer (25) — Aparigraha (non-accumulation)
# Fires before implementation. Direction-of-thought, not rigid rule.
# --------------------------------------------------------------------------
pragmatism:
  - name: reuse-first
    path: skills/25-pragmatism/reuse-first/SKILL.md
    description: Before writing any new utility, scan the codebase and declared dependencies for an equivalent. Reuse only after fit and edge-case validation.
    tags: [pragmatism, reuse, aparigraha, edge-cases, brownfield, check-before-create]
    reasoning_mode: plan-execute

  - name: dependency-utility-scout
    path: skills/25-pragmatism/dependency-utility-scout/SKILL.md
    description: Mine the project's declared dependencies and produce a per-capability inventory of utilities the team already imports — advisory, never auto-rewrites.
    tags: [pragmatism, dependencies, inventory, leverage, brownfield, advisory]
    reasoning_mode: plan-execute

  - name: style-conformance
    path: skills/25-pragmatism/style-conformance/SKILL.md
    description: Detect the project's existing conventions (naming, formatting, errors, logging, tests) and produce a house-style profile downstream skills consult — awareness, not enforcement.
    tags: [pragmatism, style, conventions, conformance, brownfield, when-in-rome]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# Implementation Layer (30)
# --------------------------------------------------------------------------
implementation:
  - name: cleanup
    path: skills/30-implementation/cleanup/SKILL.md
    description: When the codebase has accumulated cruft — removes noise, enforces formatting, safely renames identifiers.
    tags: [code-quality, formatting, renaming]
    reasoning_mode: mixed

  - name: api-implementation
    path: skills/30-implementation/api-implementation/SKILL.md
    description: When the API contract is ready and needs code behind it — implements handlers/controllers with validation.
    tags: [implementation, api, handlers, validation]
    reasoning_mode: plan-execute

  - name: data-access
    path: skills/30-implementation/data-access/SKILL.md
    description: When your API is fast but your database calls aren't — repository pattern, query optimization, N+1 prevention.
    tags: [implementation, database, repository, queries]
    reasoning_mode: plan-execute

  - name: refactoring-suite
    path: skills/30-implementation/refactoring-suite/SKILL.md
    description: When you need to restructure code without breaking behavior — complex refactors with strict test verification.
    tags: [implementation, refactoring, extract, rename]
    reasoning_mode: plan-execute

  - name: error-handling
    path: skills/30-implementation/error-handling/SKILL.md
    description: When every module handles errors differently — standardizes error types, logging, and response formats.
    tags: [implementation, errors, logging, resilience]
    reasoning_mode: linear

  - name: code-generation
    path: skills/30-implementation/code-generation/SKILL.md
    description: When you have a spec and need idiomatic code — generates from specs with language convention enforcement.
    tags: [implementation, codegen, templates, dry]
    reasoning_mode: plan-execute

  - name: refactoring
    path: skills/30-implementation/refactoring/SKILL.md
    description: When code needs restructuring but tests must stay green — safe refactoring with test-first verification.
    tags: [implementation, refactoring, safety]
    reasoning_mode: plan-execute

  - name: tdd-workflow
    path: skills/30-implementation/tdd-workflow/SKILL.md
    description: When you want tests to drive the design — Red-Green-Refactor cycle with coverage targets.
    tags: [implementation, tdd, testing, workflow]
    reasoning_mode: plan-execute

  - name: resilience-patterns
    path: skills/30-implementation/resilience-patterns/SKILL.md
    description: Circuit breaker, retry with backoff, bulkhead isolation, fallback chains, timeouts.
    tags: [implementation, resilience, circuit-breaker, retry, bulkhead, fallback]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Quality Layer (40)
# --------------------------------------------------------------------------
quality:
  - name: test-strategy
    path: skills/40-quality/test-strategy/SKILL.md
    description: When you need a testing philosophy, not just tests — defines the pyramid, coverage targets, and conventions.
    tags: [quality, testing, strategy, coverage]
    reasoning_mode: plan-execute

  - name: unit-testing
    path: skills/40-quality/unit-testing/SKILL.md
    description: When a function needs proof it works — generates unit tests for Jest, JUnit, Go Test, PyTest.
    tags: [quality, testing, unit, tdd]
    reasoning_mode: linear

  - name: integration-testing
    path: skills/40-quality/integration-testing/SKILL.md
    description: When unit tests pass but the system doesn't work — tests across component boundaries with Docker containers.
    tags: [quality, testing, integration, docker]
    reasoning_mode: plan-execute

  - name: mutation-testing
    path: skills/40-quality/mutation-testing/SKILL.md
    description: When you suspect tests pass but don't actually catch bugs — verifies test quality by injecting faults.
    tags: [quality, testing, mutation, coverage]
    reasoning_mode: plan-execute

  - name: code-review
    path: skills/40-quality/code-review/SKILL.md
    description: When a PR needs more than a thumbs-up — review checklists, defect categories, severity classification.
    tags: [quality, review, pr, defects]
    reasoning_mode: plan-execute

  - name: testing-strategy
    path: skills/40-quality/testing-strategy/SKILL.md
    description: When you're unsure what to test next — coverage analysis, test type selection, boundary condition identification.
    tags: [quality, testing, strategy, coverage]
    reasoning_mode: plan-execute

  - name: security-review
    path: skills/40-quality/security-review/SKILL.md
    description: When security can't be an afterthought — OWASP Top 10 scan, vulnerability detection, secret scanning.
    tags: [quality, security, owasp, review]
    reasoning_mode: plan-execute

  - name: performance-review
    path: skills/40-quality/performance-review/SKILL.md
    description: When the app is slow but you don't know where — bottleneck identification, complexity analysis, caching review.
    tags: [quality, performance, review, optimization]
    reasoning_mode: plan-execute

  - name: two-pass-analysis
    path: skills/40-quality/two-pass-analysis/SKILL.md
    description: When you need metrics without false-failing the build — separates collection (Pass 1) from gating (Pass 2).
    tags: [quality, analysis, metrics, gating, two-pass, linting]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Performance Layer (50)
# --------------------------------------------------------------------------
performance:
  - name: profiling-analysis
    path: skills/50-performance/profiling-analysis/SKILL.md
    description: When something is slow and you need proof of where — analyzes logs/traces to find bottlenecks and hotspots.
    tags: [performance, profiling, flame-graph, optimization]
    reasoning_mode: plan-execute

  - name: caching-strategy
    path: skills/50-performance/caching-strategy/SKILL.md
    description: When the same expensive query runs hundreds of times — implements caching patterns with proper invalidation.
    tags: [performance, caching, redis, invalidation]
    reasoning_mode: plan-execute

  - name: db-tuning
    path: skills/50-performance/db-tuning/SKILL.md
    description: When queries are the bottleneck — index analysis, query plan optimization, connection pooling.
    tags: [performance, database, indexing, queries]
    reasoning_mode: plan-execute

  - name: progressive-execution
    path: skills/50-performance/progressive-execution/SKILL.md
    description: Fast approximate results (Phase 1) then slow enriched results (Phase 2).
    tags: [performance, progressive, two-phase, rendering, latency]
    reasoning_mode: linear

  - name: resource-awareness
    path: skills/50-performance/resource-awareness/SKILL.md
    description: Monitor constraints (memory, CPU, time, rate limits), adapt behavior gracefully.
    tags: [performance, resources, memory, adaptive, degradation, budgets]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Documentation Layer (50)
# --------------------------------------------------------------------------
documentation:
  - name: api-documentation
    path: skills/50-documentation/api-documentation/SKILL.md
    description: When consumers need to know how to call your API — endpoint docs, OpenAPI specs, request/response examples.
    tags: [documentation, api, openapi, examples]
    reasoning_mode: linear

  - name: inline-documentation
    path: skills/50-documentation/inline-documentation/SKILL.md
    description: When code needs to explain itself to future readers — JSDoc/docstrings, comment quality, self-documenting patterns.
    tags: [documentation, comments, jsdoc, docstrings]
    reasoning_mode: linear

  - name: decision-records
    path: skills/50-documentation/decision-records/SKILL.md
    description: When future-you needs to know why past-you chose this — ADR authoring with context, decision, consequences.
    tags: [documentation, adr, decisions, records]
    reasoning_mode: plan-execute

  - name: changelog-generation
    path: skills/50-documentation/changelog-generation/SKILL.md
    description: When users need to know what changed and why — conventional commits parsing, semantic versioning, release notes.
    tags: [documentation, changelog, versioning, semver]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Security Layer (60)
# --------------------------------------------------------------------------
security:
  - name: threat-modeling
    path: skills/60-security/threat-modeling/SKILL.md
    description: Before attackers find the holes — identifies STRIDE threats, assesses risk, plans mitigations.
    tags: [security, threat-model, stride, risk]
    reasoning_mode: plan-execute

  - name: secure-coding-review
    path: skills/60-security/secure-coding-review/SKILL.md
    description: When code needs a security audit before shipping — scans for OWASP Top 10 vulnerabilities and anti-patterns.
    tags: [security, owasp, code-review, vulnerabilities]
    reasoning_mode: plan-execute

  - name: auth-implementation
    path: skills/60-security/auth-implementation/SKILL.md
    description: When you need auth that won't become a breach headline — implements JWT, OAuth2, or RBAC safely.
    tags: [security, auth, jwt, oauth2, rbac]
    reasoning_mode: plan-execute

  - name: dependency-scanning
    path: skills/60-security/dependency-scanning/SKILL.md
    description: When your supply chain is only as strong as its weakest dep — checks for CVEs and license compliance.
    tags: [security, dependencies, cve, sbom]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Debugging Layer (60)
# --------------------------------------------------------------------------
debugging:
  - name: root-cause-analysis
    path: skills/60-debugging/root-cause-analysis/SKILL.md
    description: When the bug is a symptom, not the cause — 5-whys, fault trees, git bisection, symptom-to-cause mapping.
    tags: [debugging, rca, fault-tree, bisection]
    reasoning_mode: plan-execute

  - name: log-analysis
    path: skills/60-debugging/log-analysis/SKILL.md
    description: When the answer is buried in 10,000 log lines — parsing, pattern recognition, event correlation, anomaly detection.
    tags: [debugging, logs, parsing, anomaly]
    reasoning_mode: plan-execute

  - name: error-handling-debug
    path: skills/60-debugging/error-handling-debug/SKILL.md
    description: When errors cascade instead of being contained — exception hierarchies, retry strategies, graceful degradation.
    tags: [debugging, errors, retry, circuit-breaker]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# DevOps Layer (70)
# --------------------------------------------------------------------------
devops:
  - name: ci-pipeline
    path: skills/70-devops/ci-pipeline/SKILL.md
    description: When you need CI that catches failures before they hit main — generates GitHub Actions/GitLab CI with artifact gating.
    tags: [devops, ci, github-actions, pipeline]
    reasoning_mode: plan-execute

  - name: docker-containerization
    path: skills/70-devops/docker-containerization/SKILL.md
    description: When your Docker image is bloated and slow to build — generates optimized multi-stage Dockerfiles.
    tags: [devops, docker, containers, images]
    reasoning_mode: linear

  - name: kubernetes-helm
    path: skills/70-devops/kubernetes-helm/SKILL.md
    description: When you need reproducible K8s deployments — generates Helm charts and Kubernetes manifests.
    tags: [devops, kubernetes, helm, deployment]
    reasoning_mode: plan-execute

  - name: terraform-iac
    path: skills/70-devops/terraform-iac/SKILL.md
    description: When infrastructure should be versioned like code — generates Terraform/OpenTofu IaC modules.
    tags: [devops, terraform, iac, cloud]
    reasoning_mode: plan-execute

  - name: monitoring-setup
    path: skills/70-devops/monitoring-setup/SKILL.md
    description: When you need to know something is broken before users tell you — SLIs/SLOs, alerting, distributed tracing.
    tags: [devops, monitoring, observability, alerting]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# Documentation Layer (80)
# --------------------------------------------------------------------------
docs:
  - name: openapi-specs
    path: skills/80-docs/openapi-specs/SKILL.md
    description: When your API docs are stale or missing — generates Swagger/OpenAPI YAML from code.
    tags: [docs, openapi, swagger, api]
    reasoning_mode: linear

  - name: readme-generation
    path: skills/80-docs/readme-generation/SKILL.md
    description: When a new contributor clones the repo and has no idea where to start — creates READMEs with usage and install guides.
    tags: [docs, readme, documentation]
    reasoning_mode: linear

  - name: release-notes
    path: skills/80-docs/release-notes/SKILL.md
    description: When release day shouldn't mean manually writing changelog — parses git logs for changelogs and version bumps.
    tags: [docs, changelog, release, versioning]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Collaboration Layer (80)
# --------------------------------------------------------------------------
collaboration:
  - name: git-workflow
    path: skills/80-collaboration/git-workflow/SKILL.md
    description: When the team needs git discipline without git fights — branching strategies, commit conventions, conflict resolution.
    tags: [collaboration, git, branching, merge]
    reasoning_mode: linear

  - name: pr-management
    path: skills/80-collaboration/pr-management/SKILL.md
    description: When PRs linger or get rubber-stamped — templates, review assignments, merge criteria that work.
    tags: [collaboration, pr, review, merge]
    reasoning_mode: linear

  - name: pair-programming
    path: skills/80-collaboration/pair-programming/SKILL.md
    description: When two heads are better than one — driver/navigator roles, mob programming, knowledge transfer protocols.
    tags: [collaboration, pairing, mob, knowledge]
    reasoning_mode: linear

  - name: knowledge-sharing
    path: skills/80-collaboration/knowledge-sharing/SKILL.md
    description: When tribal knowledge walks out the door with departing teammates — wikis, runbooks, onboarding guides.
    tags: [collaboration, docs, wikis, runbooks]
    reasoning_mode: linear

# --------------------------------------------------------------------------
# Maintenance Layer (90)
# --------------------------------------------------------------------------
maintenance:
  - name: incident-response
    path: skills/90-maintenance/incident-response/SKILL.md
    description: When production is down and the clock is ticking — triage, mitigate, and resolve outages with postmortem.
    tags: [maintenance, incident, outage, postmortem]
    reasoning_mode: plan-execute

  - name: legacy-upgrade
    path: skills/90-maintenance/legacy-upgrade/SKILL.md
    description: When the framework is 3 versions behind and nobody wants to touch it — plans major upgrades safely.
    tags: [maintenance, upgrade, migration, legacy]
    reasoning_mode: plan-execute

  - name: dependency-updates
    path: skills/90-maintenance/dependency-updates/SKILL.md
    description: When dependencies drift and Dependabot floods you with PRs — automated updates with semver and lockfile discipline.
    tags: [maintenance, dependencies, updates, semver]
    reasoning_mode: linear

  - name: deprecation-management
    path: skills/90-maintenance/deprecation-management/SKILL.md
    description: When old APIs need to die without breaking consumers — sunset timelines, migration paths, impact analysis.
    tags: [maintenance, deprecation, sunset, migration]
    reasoning_mode: plan-execute

  - name: migration-planning
    path: skills/90-maintenance/migration-planning/SKILL.md
    description: When you need to move data without losing it — version upgrades, data migration, rollback strategies.
    tags: [maintenance, migration, rollback, upgrade]
    reasoning_mode: plan-execute

  - name: tech-debt-tracking
    path: skills/90-maintenance/tech-debt-tracking/SKILL.md
    description: When tech debt is everywhere but you can only fix some — categorization, impact scoring, payoff prioritization.
    tags: [maintenance, tech-debt, tracking, prioritization]
    reasoning_mode: plan-execute

  - name: repo-maintenance
    path: skills/90-maintenance/repo-maintenance/SKILL.md
    description: Adaptive cleanup framework — start simple, discover value, pivot toward greater good.
    tags: [maintenance, cleanup, repo, optimization, adaptive, greedy]
    reasoning_mode: plan-execute

# --------------------------------------------------------------------------
# Templates
# --------------------------------------------------------------------------
templates:
  - name: skill-template
    path: templates/skill-template.md
    description: Boilerplate for creating new skills.
    tags: [meta, template]
